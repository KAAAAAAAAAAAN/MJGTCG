{
  "name": "mjgtcg-web",
  "private": true,
  "type": "module",
  "scripts": {
    "sync-images": "node -e \"require('fs').cpSync('../images','static/images',{recursive:true})\"",
    "predev": "npm run sync-images",
    "prebuild": "npm run sync-images",
    "dev": "vite dev --port 5174",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.6",
    "@sveltejs/kit": "^2.8.0",
    "@sveltejs/vite-plugin-svelte": "^3.1.2",
    "svelte": "^4.2.19",
    "svelte-check": "^3.8.6",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "colyseus.js": "^0.15.0"
  }
}
